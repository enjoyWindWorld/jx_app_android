<com.kxw.smarthome.view.RippleBackground
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="#ffffff"
    android:layout_height="match_parent"
    android:id="@+id/content"
    app:rb_color="#221bb6ef"
    app:rb_radius="32dp"
    app:rb_rippleAmount="10"
    app:rb_duration="5000"
    app:rb_scale="10">
    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_centerInParent="true"
        android:id="@+id/centerImage"
        android:src="@drawable/appliance_repair_onclick"/>
    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/foundDevice"
        android:layout_above="@id/centerImage"
        android:layout_marginBottom="32dp"
        android:layout_toLeftOf="@id/centerImage"
        android:layout_marginRight="6dp"
        android:src="@drawable/appliance_repair_onclick"
        android:visibility="invisible"/>
</com.kxw.smarthome.view.RippleBackground>